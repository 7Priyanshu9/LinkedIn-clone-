const INITIAL_STATE = {
    user:null,
};
const userReducer = (state= INITIAL_STATE,action) =>{
    switch(action.type){
        //we use this to increment the no. of likes and comment 
        default:
            return state;
    }
};

export default userReducer;